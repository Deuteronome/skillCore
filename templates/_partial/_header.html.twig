<header class="container-fluid m-0 p-0">

    <div class="row d-none d-lg-flex  border-bottom-custom align-items-center py-1 ps-3" id="header-top">
        <div class="col-1">
        {% if app.user %}
            <img src="{{asset('images/logo/'~ app.user.site.structure.logo)}}" alt="logo" class="img-fluid">           
        {% else %}
            <img src="{{asset('images/logo/default.webp')}}" alt="logo" class="img-fluid">
        {% endif %}
        </div>
        <h1 class="col-5 fw-bold ps-4">Plateforme APC</h1>
        <div class="back-anim" id="back-anim-dark1"></div>
        <div class="back-anim" id="back-anim-dark2"></div>
        <div class="back-anim" id="back-anim-mid"></div>
        <div class="back-anim" id="back-anim-light"></div>
    </div>

    <nav class="navbar d-lg-none d-flex navbar-custom border-bottom-custom" data-bs-theme="dark">
        <div class="container-fluid">
            <div class="row">
                <div class="col-2">
                    {% if app.user %}
                        <img src="{{asset('images/logo/'~ app.user.site.structure.logo)}}" alt="logo" class="img-fluid">           
                    {% else %}
                        <img src="{{asset('images/logo/default.webp')}}" alt="logo" class="img-fluid">
                    {% endif %}
                </div>
                <h1 class="col-9 navbar-brand text-custom-simple fw-bold m-0">Plateforme APC</h1>
            </div>            
        </div>
    </nav>
    
    {% include './_partial/_navbar.html.twig' %}
</header>